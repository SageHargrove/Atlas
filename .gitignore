node_modules/

# secrets — never commit
.env
.env.*
!.env.example
server/certs/
*.pem
*.key

# user data — never commit
server/users.json
server/data.json
server/data.json.tmp
server/data-*.json
server/ai-usage.json
*.json.tmp
# résumés hold a real name, address, phone and work history. DATA_DIR defaults to
# server/, so a local run drops them straight into the repo — keep them out.
resume-*.pdf
resume-*.pdf.tmp

# build output / scratch
client/dist/
*.tgz
__smoke*

# personal runbook with real server details — never commit
SETUP.local.md

# deploy artifact
atlas.tar.gz
